import React from "react";
import ReactDOM from "react-dom/client";
import BottlerSimulatorUI from "./BottlerSimulatorUI";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BottlerSimulatorUI />
  </React.StrictMode>
);
